List of Entities

* Entities
  + Players
  + Characters
  + Classes
  + Races
  + Alignments
  + Spells
  + Monsters
  + Bosses
  + NPCs
  + Equipment
  + Events
  + Achievments

```{mermaid}
erDiagram
    User  {
         INT ID PK
         VARCHAR(50) FirstName
         VARCHAR(50) LastName
         VARCHAR(100) Email
    }
    Character {
        INT ID PK
        INT UserID FK
        VARCHAR(50) Name
        INT Level
        INT ClassID FK
        INT XP
        INT RACE FK
        INT HP
        INT Mana
        INT allignmentID FK
    
    }
    Allignment {
        INT ID PK
        VARCHAR(50) Name
        
    }
    Class {
        INT ID PK
        VARCHAR(50) Name
        INT HitDie
        TEXT Description
    }
    Spell{
        INT  ID
        VARCHAR(50) Name
        INT  Level
        TEXT Effect
        INT  CastingTime
        INT  Duration
    }
    Monsters {
        string ID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    Bosses {
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    NPCs {
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    Equipment {
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    Events {
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    Achievments {
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
```
