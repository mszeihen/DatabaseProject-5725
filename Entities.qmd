List of Entities

* Entities
  + Players
  + Characters
  + Classes
  + Races
  + Alignments
  + Spells
  + Monsters
  + Bosses
  + NPCs
  + Equipment
  + Events
  + Achievments

```{mermaid}
erDiagram
    User  {
         INT(PK) UserID P
         VARCHAR(50) FirstName
         VARCHAR(50) LastName
         VARCHAR(100) Email
    }
    Character {
        INT(PK) CharacterID PK
        INT(FK) UserID
        VARCHAR(50) Name
        INT Level
        INT(FK) ClassID
        INT XP
        INT(FK) RACE
        INT HP
        INT Mana
        INT(FK) allignmentID
    
    }
    Allignment {
        INT(PK) alignmentID
        VARCHAR(50) Name
        
    }
    Class {
        INT(PK) classID
        VARCHAR(50) Name
        INT HitDie
        TEXT Description
    }
    Spell{
        INT(PK) spellID
        VARCHAR(50) Name
        INT Level
        TEXT Effect
        INT CastingTime
        INT Duration
    }
    Monsters {
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    Bosses {
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    NPCs {
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    Equipment {
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    Events {
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    Achievments {
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
```
