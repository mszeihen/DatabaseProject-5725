List of Entities

* Entities
  + Players
  + Characters
  + Classes
  + Races
  + Alignments
  + Spells
  + Monsters
  + Bosses
  + NPCs
  + Equipment
  + Events
  + Achievments

```{mermaid}
erDiagram
    User{
         int ID PK
         varchar FirstName
         varchar LastName
         varchar Email
    }
    Character{
        int ID PK
        int userID FK
        varchar Name
        int Level
        int classID FK
        int XP
        int raceACE FK
        int HP
        int mana
        int allignmentID FK
    }
    Allignment{
        int ID PK
        varchar(50) Name  
    }
    Class{
        int ID PK
        varchar(50) Name
        int HitDie
        text Description
    }
    Spell{
        int  ID
        varchar Name
        int  Level
        text Effect
        int  CastingTime
        int  Duration
    }
```
```{mermaid}
erDiagram
    Monsters{
        string ID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    Bosses{
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    NPC{
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    Equipment{
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
```
```{mermaid}
erDiagram
    Events{
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
    Achievments{
        string ProjectID PK
        string ProjectName
        string StartDate
        string EndDate
    }
```
